# Application
quarkus.application.name=datanest
quarkus.oidc.roles.role-claim-path=realm_access/roles

# Mixdeck routes authenticate by bearer token only (see MixdeckTenantResolver). Same realm as the
# default tenant, which stays application-type=web-app for the 42next admin FE.
quarkus.oidc.mixdeck.auth-server-url=${quarkus.oidc.auth-server-url}
quarkus.oidc.mixdeck.client-id=${quarkus.oidc.client-id}
quarkus.oidc.mixdeck.token.issuer=${quarkus.oidc.auth-server-url}
quarkus.oidc.mixdeck.application-type=service

# The OTP login endpoints are the way a token is obtained, so they cannot require one.
quarkus.http.auth.permission.otp-auth.paths=/datanest/auth/otp/*
quarkus.http.auth.permission.otp-auth.policy=permit
quarkus.package.jar.type=uber-jar
quarkus.http.port=38799
quarkus.http.limits.max-body-size=1G
quarkus.index-dependency.core.group-id=com.semantyca
quarkus.index-dependency.officeframe.group-id=com.semantyca
quarkus.arc.exclude-types=com.semantyca.core.jwt.JWTCallerPrincipalFactory,io.smallrye.jwt.auth.cdi.JWTCallerPrincipalFactoryProducer

# Suppress verbose FFmpeg wrapper output
quarkus.log.category."net.bramp.ffmpeg".level=WARN
quarkus.log.category."net.bramp.ffmpeg.builder".level=ERROR
quarkus.log.category."net.bramp.ffmpeg.RunProcessFunction".level=ERROR
